[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm install', 'cd client && npm install']

[phases.build]
cmds = ['cd client && npm run build']

[start]
cmd = 'node src/server.js'
# Force rebuild - Fri Feb 27 14:04:38 UTC 2026
